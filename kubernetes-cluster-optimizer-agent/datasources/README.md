# Datasources

This directory contains data source configurations for connecting to various Kubernetes monitoring systems like Prometheus, Kubernetes API, Grafana, etc.

Each data source is defined in a separate file that specifies connection details and configuration.