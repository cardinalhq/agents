# Datasources

This directory contains data source configurations for connecting to various AWS data systems like Cost Explorer, CloudWatch, Athena, etc.

Each data source is defined in a separate file that specifies connection details and configuration.