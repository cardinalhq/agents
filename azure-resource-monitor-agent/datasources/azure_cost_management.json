{
  "name": "Azure Cost Management",
  "type": "azure_cost_management",
  "connection": {
    "tenant_id": "${AZURE_TENANT_ID}",
    "client_id": "${AZURE_CLIENT_ID}",
    "client_secret": "${AZURE_CLIENT_SECRET}",
    "subscription_id": "${AZURE_SUBSCRIPTION_ID}"
  }
}