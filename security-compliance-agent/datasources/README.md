# Datasources

This directory contains data source configurations for connecting to various security and compliance systems like AWS Config, Azure Security Center, vulnerability scanners, etc.

Each data source is defined in a separate file that specifies connection details and configuration.