# Datasources

This directory contains data source configurations for connecting to various infrastructure systems like cloud APIs, Terraform state, version control, etc.

Each data source is defined in a separate file that specifies connection details and configuration.